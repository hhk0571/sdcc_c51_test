#include "lib_lcd1602.h"


/*------------------------------------------------
              写入单个字符函数
x 横坐标(那个位置): 有效值 0 - 39
y 纵坐标(第几行)  : 有效值 0 - 1
------------------------------------------------*/
void lcd_pos_char(byte x, byte y, byte Data)
{
   lcd_pos (x, y);
   lcd_write_dat(Data);
}
